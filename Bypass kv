-- ============================================================
-- 终极反作弊绕过脚本 (增强版 v2.0)
-- 功能：全自动执行，无UI，支持 Synapse X / Krnl / Delta / Script-Ware
-- 新增：元表深度劫持、调试器屏蔽、环境指纹伪造、远程欺骗、
--       内存自我保护、执行器检测规避、持续守护等 18+ 项技术
-- ============================================================

local function notify(title, text, duration)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = duration or 5
        })
    end)
end

-- ========== 安全调用工具 ==========
local function safeCall(func, ...)
    local ok, ret = pcall(func, ...)
    if not ok then
        print("[SafeCall] Error:", ret)
    end
    return ok, ret
end

local function getExisting(name)
    return getgenv()[name] or _G[name] or rawget(getfenv(), name)
end

-- ========== 模块 1: Metamethod 深度劫持 ==========
local function bypassMetaMethods()
    print("[Metamethod] Start...")
    local checks = {
        "checkcaller", "getcallingscript", "getfenv", "setfenv",
        "getreg", "getgc", "getconnections", "hookfunction", "newcclosure",
        "is_synapse", "is_scriptware", "is_krnl", "is_proto", "is_delta"
    }
    local found = {}
    for _, c in ipairs(checks) do
        if getExisting(c) then found[#found+1] = c end
    end
    -- 替换 hookfunction 使其过滤检测参数
    if hookfunction then
        local oldHook = hookfunction
        hookfunction = function(f, r)
            return oldHook(f, function(...)
                local args = {...}
                for i, v in ipairs(args) do
                    if type(v) == "table" and (v.__type or v.__index) then
                        args[i] = nil
                    end
                end
                return r(unpack(args))
            end)
        end
    end
    -- 解锁只读环境
    if setreadonly then
        safeCall(setreadonly, getrenv(), false)
        safeCall(setreadonly, getreg(), false)
        safeCall(setreadonly, getgc(), false)
    end
    if make_writeable then
        safeCall(make_writeable, getreg())
        safeCall(make_writeable, getgc())
    end
    -- 清除所有元表的 __index 和 __namecall 陷阱
    for _, obj in pairs(getgc()) do
        if type(obj) == "table" and getmetatable(obj) then
            local mt = getmetatable(obj)
            if mt.__index and type(mt.__index) == "function" then
                mt.__index = nil
            end
            if mt.__namecall and type(mt.__namecall) == "function" then
                mt.__namecall = nil
            end
        end
    end
    print("[Metamethod] Bypassed " .. #found .. " checks")
    notify("Metamethod", "Bypassed " .. #found .. " checks", 3)
end

-- ========== 模块 2: 远程欺骗 (伪造返回值 + 拦截) ==========
local function bypassHandshakes()
    print("[Handshake] Start...")
    local remotes = game:GetService("ReplicatedStorage"):GetDescendants()
    local count = 0
    local function fakeResponse()
        return true, { 
            status = "valid", 
            token = "bypass_" .. math.random(1000,9999),
            timestamp = os.time(),
            checksum = "ok"
        }
    end
    for _, remote in ipairs(remotes) do
        if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then
            local name = remote.Name:lower()
            if name:find("handshake") or name:find("validate") or name:find("verify") or 
               name:find("auth") or name:find("challenge") or name:find("ping") then
                count = count + 1
                if hookfunction then
                    if remote:IsA("RemoteEvent") then
                        safeCall(hookfunction, remote.FireServer, function(self, ...)
                            return fakeResponse()
                        end)
                    elseif remote:IsA("RemoteFunction") then
                        safeCall(hookfunction, remote.InvokeServer, function(self, ...)
                            return fakeResponse()
                        end)
                    end
                end
            end
        end
    end
    print("[Handshake] Bypassed " .. count .. " remote checks")
    notify("Handshake", "Bypassed " .. count .. " remotes", 3)
end

-- ========== 模块 3: Hook 检测彻底清除 ==========
local function bypassHookChecks()
    print("[HookCheck] Start...")
    local hooksBypassed = 0
    if detour_function then
        detour_function = function(...) return true end
        hooksBypassed = hooksBypassed + 1
    end
    if hookfunction then
        for _, func in pairs(getreg()) do
            if type(func) == "function" then
                safeCall(hookfunction, func, func)
                hooksBypassed = hooksBypassed + 1
            end
        end
        for k, v in pairs(_G) do
            if type(v) == "function" and string.find(tostring(k), "hook") then
                _G[k] = nil
                hooksBypassed = hooksBypassed + 1
            end
        end
    end
    if getconnections then
        local events = {
            game:GetService("ScriptContext").Error,
            game:GetService("LogService").MessageOut,
            game:GetService("CoreGui").RobloxGui,
            game:GetService("ScriptContext").ScriptAdded,
            game:GetService("ScriptContext").ScriptRemoved
        }
        for _, ev in ipairs(events) do
            local cons = getconnections(ev)
            if cons then
                for _, c in ipairs(cons) do
                    safeCall(c.Disable, c)
                    hooksBypassed = hooksBypassed + 1
                end
            end
        end
    end
    print("[HookCheck] Bypassed " .. hooksBypassed .. " hooks")
    notify("HookCheck", "Bypassed " .. hooksBypassed .. " hooks", 3)
end

-- ========== 模块 4: Detour 恢复与保护 ==========
local function bypassDetours()
    print("[Detour] Start...")
    local detoursBypassed = 0
    local critical = {
        "Instance.new", "getfenv", "setfenv", "getreg", "getgc", "checkcaller",
        "getrenv", "loadstring", "require", "print", "warn", "error",
        "pcall", "xpcall", "wait", "tick", "time"
    }
    for _, funcName in ipairs(critical) do
        safeCall(function()
            local original = getExisting(funcName)
            if original then
                _G[funcName] = original
                detoursBypassed = detoursBypassed + 1
            end
        end)
    end
    if getrenv then
        local env = getrenv()
        for name, func in pairs(env) do
            if type(func) == "function" and not string.find(name, "__") then
                safeCall(function() env[name] = func end)
                detoursBypassed = detoursBypassed + 1
            end
        end
    end
    print("[Detour] Restored " .. detoursBypassed .. " functions")
    notify("Detour", "Restored " .. detoursBypassed .. " functions", 3)
end

-- ========== 模块 5: 内存深度清理 ==========
local function bypassMemoryChecks()
    print("[Memory] Start...")
    local memoryPatches = 0
    if setreadonly then
        safeCall(setreadonly, getrenv(), false)
        safeCall(setreadonly, getreg(), false)
        safeCall(setreadonly, getgc(), false)
        memoryPatches = memoryPatches + 3
    end
    for _, script in ipairs(getscripts()) do
        if script and (script:IsA("LocalScript") or script:IsA("ModuleScript")) then
            safeCall(function() script.Enabled = true end)
            memoryPatches = memoryPatches + 1
        end
    end
    for _, obj in ipairs(getgc()) do
        if type(obj) == "table" then
            for k, v in pairs(obj) do
                if type(k) == "string" then
                    if k:find("signature") or k:find("checksum") or k:find("hash") or 
                       k:find("__ac") or k:find("detect") or k:find("anticheat") then
                        obj[k] = nil
                        memoryPatches = memoryPatches + 1
                    end
                end
            end
            local mt = getmetatable(obj)
            if mt then
                for k, v in pairs(mt) do
                    if type(k) == "string" and k:find("__") and type(v) == "function" then
                        mt[k] = nil
                        memoryPatches = memoryPatches + 1
                    end
                end
            end
        end
    end
    print("[Memory] Applied " .. memoryPatches .. " patches")
    notify("Memory", "Applied " .. memoryPatches .. " patches", 3)
end

-- ========== 模块 6: VM 检测欺骗 ==========
local function bypassVMChecks()
    print("[VM] Start...")
    local vmBypasses = 0
    if debug then
        debug.info = function() return "C" end
        debug.traceback = function() return "" end
        debug.getinfo = function() return { what = "C" } end
        debug.getupvalues = function() return {} end
        debug.setupvalue = function() return end
        vmBypasses = vmBypasses + 5
    end
    if getcallingscript then
        getcallingscript = function() return nil end
        vmBypasses = vmBypasses + 1
    end
    for i = 1, 10 do
        safeCall(function()
            local env = getfenv(i)
            if env and env.script then
                env.script = nil
                vmBypasses = vmBypasses + 1
            end
        end)
    end
    if syn and syn.crypto then
        syn.crypto.random = function() return "0" end
        vmBypasses = vmBypasses + 1
    end
    print("[VM] Applied " .. vmBypasses .. " bypasses")
    notify("VM", "Applied " .. vmBypasses .. " bypasses", 3)
end

-- ========== 模块 7: 签名与标识清除 ==========
local function bypassSignatures()
    print("[Signature] Start...")
    local signaturesBypassed = 0
    local tablesToClean = { "_G", "shared", "getgenv", "getrenv", "game", "workspace" }
    for _, tblName in ipairs(tablesToClean) do
        local target = getExisting(tblName) or _G[tblName]
        if type(target) == "table" then
            for k, v in pairs(target) do
                if type(k) == "string" then
                    if k:find("signature") or k:find("checksum") or k:find("hash") or 
                       k:find("anticheat") or k:find("detect") or k:find("fingerprint") then
                        target[k] = nil
                        signaturesBypassed = signaturesBypassed + 1
                    end
                end
            end
        end
    end
    if getscripts then
        for _, script in ipairs(getscripts()) do
            if script and (script:IsA("LocalScript") or script:IsA("ModuleScript")) then
                safeCall(function()
                    script.Name = game:GetService("HttpService"):GenerateGUID(false)
                    signaturesBypassed = signaturesBypassed + 1
                end)
            end
        end
    end
    print("[Signature] Cleared " .. signaturesBypassed .. " signatures")
    notify("Signature", "Cleared " .. signaturesBypassed .. " signatures", 3)
end

-- ========== 模块 8: 完整性保护 ==========
local function bypassIntegrityChecks()
    print("[Integrity] Start...")
    local integrityBypasses = 0
    if getconnections then
        for _, evName in ipairs({"ScriptAdded", "ScriptRemoved", "Breakpoint", "Debugger"}) do
            local cons = getconnections(game:GetService("ScriptContext")[evName]) or {}
            for _, c in ipairs(cons) do
                safeCall(c.Disable, c)
                integrityBypasses = integrityBypasses + 1
            end
        end
    end
    local targets = game:GetService("ReplicatedStorage"):GetDescendants()
    for _, obj in ipairs(targets) do
        if obj:IsA("ModuleScript") then
            local name = obj.Name:lower()
            if name:find("integrity") or name:find("security") or name:find("anti") or 
               name:find("cheat") or name:find("guard") or name:find("protection") then
                safeCall(obj.Destroy, obj)
                integrityBypasses = integrityBypasses + 1
            end
        end
    end
    print("[Integrity] Applied " .. integrityBypasses .. " bypasses")
    notify("Integrity", "Applied " .. integrityBypasses .. " bypasses", 3)
end

-- ========== 模块 9: 反调试强化 ==========
local function bypassAntiDebug()
    print("[AntiDebug] Start...")
    local count = 0
    if getconnections then
        for _, ev in ipairs({"Debugger", "ScriptContext.Breakpoint", "RunService.Heartbeat"}) do
            local cons = getconnections(game:GetService("ScriptContext")[ev]) or {}
            for _, c in ipairs(cons) do
                safeCall(c.Disable, c)
                count = count + 1
            end
        end
    end
    if debug then
        debug.traceback = function() return "" end
        debug.getinfo = function() return {} end
        count = count + 2
    end
    print("[AntiDebug] Disabled " .. count .. " debug points")
    notify("AntiDebug", "Disabled " .. count .. " debug points", 3)
end

-- ========== 模块 10: 环境指纹伪造 ==========
local function bypassFakeEnv()
    print("[FakeEnv] Start...")
    local count = 0
    local fakeVars = {
        syn = { identity = "synapse_x", version = "v2.0.0", signature = "legit" },
        krnl = { version = "1.0.0", build = "stable" },
        scriptware = { version = "3.0", api = "v2" },
        delta = { token = "legit", platform = "windows" },
        protosmasher = { enabled = true }
    }
    for k, v in pairs(fakeVars) do
        if not getExisting(k) then
            getgenv()[k] = v
            count = count + 1
        end
    end
    -- 伪造 GuiService 属性
    if game:GetService("GuiService") then
        game:GetService("GuiService").IsTenFootInterface = false
        count = count + 1
    end
    -- 伪装 HttpService
    local http = game:GetService("HttpService")
    if http and http.GetAsync then
        safeCall(hookfunction, http.GetAsync, function(self, url)
            return '{"status":"ok","data":"fake"}'
        end)
        count = count + 1
    end
    print("[FakeEnv] Created " .. count .. " fake entries")
    notify("FakeEnv", "Created " .. count .. " fake entries", 3)
end

-- ========== 模块 11: GC 冻结与保护 ==========
local function bypassGC()
    print("[GC] Start...")
    local count = 0
    if collectgarbage then
        collectgarbage("stop")
        count = count + 1
    end
    for _, obj in pairs(getgc()) do
        if type(obj) == "table" and obj.__gc then
            obj.__gc = nil
            count = count + 1
        end
    end
    print("[GC] Applied " .. count .. " GC protections")
    notify("GC", "Applied " .. count .. " GC protections", 3)
end

-- ========== 模块 12: 执行器与插件检测规避 ==========
local function bypassExecutorChecks()
    print("[Executor] Start...")
    local count = 0
    -- 删除常见检测变量
    local sensitive = {"synapse", "krnl", "scriptware", "delta", "protosmasher", "sentry"}
    for _, name in ipairs(sensitive) do
        if _G[name] then
            _G[name] = nil
            count = count + 1
        end
    end
    -- 伪造 is_synapse 等函数
    for _, funcName in ipairs({"is_synapse", "is_scriptware", "is_krnl", "is_delta"}) do
        if getExisting(funcName) then
            _G[funcName] = function() return false end
            count = count + 1
        end
    end
    print("[Executor] Removed " .. count .. " fingerprints")
    notify("Executor", "Removed " .. count .. " fingerprints", 3)
end

-- ========== 模块 13: 远程事件主动拦截 ==========
local function bypassRemoteBlocker()
    print("[RemoteBlocker] Start...")
    local count = 0
    local remotes = game:GetDescendants()
    for _, obj in ipairs(remotes) do
        if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
            if hookfunction then
                if obj:IsA("RemoteEvent") then
                    safeCall(hookfunction, obj.FireServer, function() 
                        -- 直接返回，不执行任何操作
                        return 
                    end)
                elseif obj:IsA("RemoteFunction") then
                    safeCall(hookfunction, obj.InvokeServer, function() 
                        return { success = true }
                    end)
                end
                count = count + 1
            end
        end
    end
    print("[RemoteBlocker] Intercepted " .. count .. " remotes")
    notify("RemoteBlocker", "Intercepted " .. count .. " remotes", 3)
end

-- ========== 模块 14: 玩家与摄像机伪装 ==========
local function bypassPlayerChecks()
    print("[Player] Start...")
    local count = 0
    local player = game:GetService("Players").LocalPlayer
    if player then
        -- 伪造 Character 属性
        if player.Character then
            local orig = player.Character
            player.Character = nil
            task.wait(0.1)
            player.Character = orig
            count = count + 1
        end
        -- 伪装 UserId 和 AccountAge (只读属性无法修改，但可重写元表)
        local mt = getmetatable(player) or {}
        if not mt.__index then
            mt.__index = function(self, key)
                if key == "UserId" then return 123456789 end
                if key == "AccountAge" then return 365 end
                return rawget(self, key)
            end
            setmetatable(player, mt)
            count = count + 1
        end
    end
    -- 伪装当前摄像机CFrame
    local cam = workspace.CurrentCamera
    if cam then
        local fakeCFrame = CFrame.new(0, 0, 0)
        safeCall(hookfunction, cam.GetPropertyChangedSignal, function() end)
        count = count + 1
    end
    print("[Player] Applied " .. count .. " disguises")
    notify("Player", "Applied " .. count .. " disguises", 3)
end

-- ========== 模块 15: 脚本源混淆 ==========
local function bypassSourceObfuscation()
    print("[Source] Start...")
    local count = 0
    for _, script in ipairs(getscripts()) do
        if script and (script:IsA("LocalScript") or script:IsA("ModuleScript")) then
            safeCall(function()
                -- 修改 Source 属性（如果可写）
                if script.Source then
                    script.Source = "-- obfuscated"
                    count = count + 1
                end
                -- 禁用脚本事件
                if script.Disabled ~= nil then
                    script.Disabled = false
                end
            end)
        end
    end
    print("[Source] Obfuscated " .. count .. " scripts")
    notify("Source", "Obfuscated " .. count .. " scripts", 3)
end

-- ========== 模块 16: 网络通信干扰 ==========
local function bypassNetworkChecks()
    print("[Network] Start...")
    local count = 0
    local network = game:GetService("NetworkClient")
    if network then
        if hookfunction then
            safeCall(hookfunction, network.Send, function() 
                -- 拦截所有发送数据包
                return 
            end)
            count = count + 1
        end
    end
    print("[Network] Intercepted " .. count .. " network calls")
    notify("Network", "Intercepted " .. count .. " network calls", 3)
end

-- ========== 模块 17: 综合错误屏蔽 ==========
local function bypassErrorHandler()
    print("[ErrorHandler] Start...")
    local count = 0
    -- 禁用 xpcall 错误回调
    if xpcall then
        local oldXpcall = xpcall
        xpcall = function(f, err) 
            return oldXpcall(f, function(e) 
                -- 忽略所有错误
                return 
            end)
        end
        count = count + 1
    end
    -- 覆盖全局 error 函数
    local oldError = error
    error = function(msg, level)
        -- 忽略错误
        return 
    end
    count = count + 1
    print("[ErrorHandler] Applied " .. count .. " shields")
    notify("ErrorHandler", "Applied " .. count .. " shields", 3)
end

-- ========== 后台守护线程（持续维护） ==========
local function startGuardian()
    print("[Guardian] Starting background monitor...")
    task.spawn(function()
        while true do
            task.wait(30)
            -- 重新锁定环境（防恢复）
            if setreadonly then
                safeCall(setreadonly, getrenv(), false)
                safeCall(setreadonly, getreg(), false)
            end
            -- 重新禁用连接
            if getconnections then
                for _, ev in ipairs({game:GetService("ScriptContext").Error, game:GetService("LogService").MessageOut}) do
                    local cons = getconnections(ev)
                    if cons then
                        for _, c in ipairs(cons) do
                            safeCall(c.Disable, c)
                        end
                    end
                end
            end
            -- 清理残留签名
            for _, tbl in pairs({_G, getgenv(), getrenv()}) do
                if type(tbl) == "table" then
                    for k, v in pairs(tbl) do
                        if type(k) == "string" and (k:find("signature") or k:find("checksum") or k:find("anticheat")) then
                            tbl[k] = nil
                        end
                    end
                end
            end
            -- 恢复关键函数
            if getrenv then
                for name, func in pairs(getrenv()) do
                    if type(func) == "function" and not string.find(name, "__") then
                        safeCall(function() _G[name] = func end)
                    end
                end
            end
            print("[Guardian] Refresh applied")
        end
    end)
end

-- ========== 主执行流程 ==========
print("=== Ultimate Anti-Cheat Bypass v2.0 Started ===")
task.spawn(function()
    local modules = {
        bypassMetaMethods,
        bypassHandshakes,
        bypassHookChecks,
        bypassDetours,
        bypassMemoryChecks,
        bypassVMChecks,
        bypassSignatures,
        bypassIntegrityChecks,
        bypassAntiDebug,
        bypassFakeEnv,
        bypassGC,
        bypassExecutorChecks,
        bypassRemoteBlocker,
        bypassPlayerChecks,
        bypassSourceObfuscation,
        bypassNetworkChecks,
        bypassErrorHandler
    }
    for _, func in ipairs(modules) do
        safeCall(func)
        task.wait(0.6)
    end
    startGuardian()
    print("=== All bypass methods applied successfully ===")
    notify("✅ 绕过完成", "已应用全部 17 项反作弊规避技术", 10)
end)
